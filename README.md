# Telegram HADS Mini App

Это мини-приложение для Telegram, написанное на Next.js. Оно открывается по кнопке в боте, позволяет пройти опрос HADS, сохраняет результаты в Supabase и рисует график изменений тревожности и депрессии.

Если вы только начинаете и никогда не разворачивали подобные проекты, выполните шаги ниже строго по порядку. После каждого шага ставьте галочку — так вы будете уверены, что ничего не пропустили.

---

## ✅ Шаг 0. Что понадобится
- Компьютер с macOS, Windows или Linux.
- Установленный [Git](https://git-scm.com/downloads).
- Установленный [Node.js 18 LTS или новее](https://nodejs.org/en/download). Node автоматически устанавливает `npm`.
- Учётные записи: GitHub, [Supabase](https://supabase.com/), [Vercel](https://vercel.com/) и уже существующий Telegram-бот.

Проверьте, что Git и Node установлены:
```bash
git --version
node --version
npm --version
```
Если команды не находятся — установите программы и повторите проверку.

---

## ✅ Шаг 1. Скопируйте проект к себе
1. Создайте **новый репозиторий** на GitHub (кнопка “New Repository”).
2. Откройте терминал/PowerShell и перейдите в папку, где хотите хранить код.
3. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/<ваш-логин>/<ваш-репозиторий>.git hads-miniapp
   cd hads-miniapp
   ```
4. Скопируйте содержимое текущего архива проекта в эту папку или загрузите файлы через GitHub (если вы в Codespace, перейдите к следующему шагу).
5. Зафиксируйте стартовое состояние (это нужно, чтобы проще отслеживать изменения):
   ```bash
   git add .
   git commit -m "Add Telegram HADS mini app scaffold"
   ```

### 📦 Если используете GitHub Desktop
1. Нажмите **File → Clone repository… → URL** и вставьте адрес `https://github.com/<ваш-логин>/<ваш-репозиторий>.git`.
2. Выберите папку, куда нужно скачать файлы, и подтвердите **Clone**.
3. После клонирования откройте папку проекта в Проводнике/Finder — там появятся все файлы. Дальше следуйте шагу 3 (создание `.env.local`) и остальным пунктам README.

> ⚠️ Если GitHub Desktop пишет «Sorry, I can’t find any repository matching…», проверьте:
> - что адрес введён без опечаток и совпадает с URL из браузера;
> - что репозиторий публичный или у вас есть к нему доступ (в браузере на GitHub он тоже должен открываться);
> - что вы вошли в GitHub Desktop под тем же аккаунтом, которому принадлежит репозиторий.
>
> Также можно нажать кнопку **Code → Open with GitHub Desktop** прямо на странице репозитория — Desktop заполнит все поля автоматически.

### ⛳ Что делать сразу после того, как локальная копия готова
1. **Перейдите к шагу 2.** Настройте Supabase: создайте проект и выполните SQL-скрипт из `supabase/schema.sql`.
2. **Продолжите шагом 3.** Создайте `.env.local` из примера и впишите туда `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
3. **Дойдите до шага 4.** Выполните `npm install`, затем `npm run dev`, чтобы убедиться, что проект запускается локально.
4. **Завершите шагом 5.** Пройдите тест, нажмите «Сохранить результат» и проверьте в Supabase, что появилась новая строка.

Когда эти пункты выполнены, переходите к деплою на Vercel (шаг 6) и привязке к Telegram-боту (шаг 7).

### 🔄 Как синхронизировать изменения с GitHub
- **Из GitHub → на ваш компьютер (pull).** Если кто-то внёс правки в репозиторий или вы хотите получить свежую версию, нажмите в GitHub Desktop **Fetch origin → Pull origin** (или выполните в терминале `git pull origin main`). После этого локальная копия обновится.
- **С вашего компьютера → на GitHub (push).** После того как вы отредактировали файлы, зафиксируйте их и отправьте:
  1. В GitHub Desktop отметьте нужные файлы, напишите описание и нажмите **Commit to main**.
  2. Нажмите **Push origin**, чтобы загрузить коммит на GitHub. В терминале команды будут такими: `git add .`, `git commit -m "Описание изменений"`, `git push origin main`.
- **Проверка.** Откройте репозиторий в браузере — новые файлы и коммиты должны появиться сразу после `push`. Если страницу обновить и изменений нет, значит `push` не был выполнен или произошла ошибка (GitHub Desktop покажет уведомление).

Эти действия нужно повторять каждый раз, когда вы хотите перенести локальные правки в GitHub или наоборот получить свежие изменения из облака.

---

## ✅ Шаг 2. Настройте Supabase
1. Войдите в Supabase и создайте новый проект: **New project → Choose organization → Create new project**.
2. Сохраните значения **Project URL** и **anon public key** — они понадобятся позже (их можно найти во вкладке **Project settings → API**).
3. Откройте раздел **SQL Editor** и вставьте содержимое файла [`supabase/schema.sql`](supabase/schema.sql). Нажмите **Run**.
   - Скрипт создаст таблицу `hads_results`, настроит политики безопасности и индексы.
4. Перейдите в **Table Editor → hads_results** и убедитесь, что таблица появилась (пока пустая).

> 💡 Советы: при необходимости можно повторно запустить скрипт — он аккуратно проверяет, существует ли таблица и индексы.

### 👉 Что делать сразу после Supabase (простым языком)
1. **Запишите ключи.** Скопируйте `Project URL` и `anon public key` в любой заметник — они пригодятся через пару минут.
2. **Создайте файл настроек.** В корне проекта сделайте `.env.local` (см. следующий шаг) и впишите туда оба значения.
3. **Поставьте зависимости и запустите сайт.** Выполните `npm install`, затем `npm run dev` — откроется локальная версия приложения.
4. **Проверьте таблицу.** Пройдите тест, нажмите «Сохранить результат» и убедитесь в Supabase, что строка появилась.

Если эти четыре пункта выполнены, значит связка «приложение ↔ Supabase» работает корректно, и можно переходить к деплою.

---

## ✅ Шаг 3. Настройте переменные окружения
1. В корне проекта создайте файл `.env.local` на основе примера:
   ```bash
   cp .env.example .env.local
   ```
2. Откройте `.env.local` в любом редакторе и подставьте сохранённые данные из Supabase:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=<ваш-anon-key>
   ```
3. Проверьте, что в конце файла нет лишних пробелов и кавычек.

---

## ✅ Шаг 4. Установите зависимости и запустите проект локально
1. В терминале (находясь в папке проекта) установите зависимости и запустите дев-сервер:
   ```bash
   npm install
   npm run dev
   ```
2. Дождитесь сообщения `Local: http://localhost:3000`.
3. Откройте браузер и перейдите на [http://localhost:3000](http://localhost:3000).
4. Убедитесь, что страница загружается. Telegram WebApp SDK автоматически определит пользователя, если приложение будет открыто из Telegram, но для теста локально всё тоже работает.

Чтобы остановить сервер, нажмите `Ctrl + C` в терминале.

---

## ✅ Шаг 5. Проверьте сохранение данных
1. В браузере нажмите **«Начать тест»**, ответьте на вопросы и нажмите **«Сохранить результат»**.
2. Откройте Supabase → **Table Editor → hads_results** и убедитесь, что появилась новая строка.
3. Вернитесь в браузер и обновите страницу — на графике должна появиться точка с вашим результатом.

Если данные не сохраняются:
- Проверьте, что `.env.local` заполнен правильно.
- Перезапустите `npm run dev`, чтобы подтянуть переменные окружения.
- Посмотрите ошибки в консоли браузера (`F12 → Console`).

---

## ✅ Шаг 6. Подготовьте деплой на Vercel
1. Загрузите код в GitHub (если ещё не сделали):
   ```bash
   git push origin main
   ```
2. Зайдите на [https://vercel.com/new](https://vercel.com/new) → **Add New Project** → выберите созданный репозиторий.
3. В блоке **Environment Variables** добавьте ключи из `.env.local`:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
4. Нажмите **Deploy** и дождитесь окончания сборки. После успешного деплоя появится URL вида `https://<ваше-приложение>.vercel.app`.

---

## ✅ Шаг 7. Подключите мини-приложение к Telegram-боту
1. Откройте диалог с [@BotFather](https://t.me/BotFather).
2. Выполните команду `/setdomain`, выберите вашего бота и укажите домен вида `https://<ваше-приложение>.vercel.app`.
3. Выполните `/setmenubutton` → выберите бота → пункт **Web App** → введите название кнопки и тот же URL.
4. Если вы используете пользовательскую клавиатуру, отправьте Bot API JSON:
   ```json
   {
     "keyboard": [
       [
         {
           "text": "Пройти тест HADS",
           "web_app": { "url": "https://<ваше-приложение>.vercel.app" }
         }
       ]
     ],
     "resize_keyboard": true
   }
   ```
5. Откройте Telegram, найдите своего бота, нажмите на кнопку — мини-приложение должно открыться.

---

## ✅ Шаг 8. Контрольный чек-лист
- [ ] Локальный запуск (`npm run dev`) работает без ошибок.
- [ ] Результаты появляются в таблице `hads_results` в Supabase.
- [ ] Приложение успешно деплоено на Vercel и открывается по публичному URL.
- [ ] Кнопка в Telegram открывает мини-приложение.

Если все пункты выполнены — вы успешно запустили проект. Поздравляем! 🎉

---

## Справка для разработчика
- Основной код находится в `src/components` и `src/lib`. Логику теста можно изменять в `src/lib/hads.ts`.
- Настройка клиента Supabase — в `src/lib/supabaseClient.ts`.
- Стили по умолчанию лежат в `src/styles/globals.css`.
- Дополнительную диагностику можно включить командой `npm run lint`.

Если понадобится изменить визуальный стиль или текст вопросов, начинайте с этих файлов.
